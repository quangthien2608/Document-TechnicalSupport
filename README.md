# Ubuntu
**1.Kiểm tra server** </br>
**Kiểm tra tài nguyên :**
- **CPU** <p>``` cat /proc/cpuinfo | grep processor ```</p>
- **RAM** <p>```cat /proc/meminfo | grep -i memtotal```</p>
- **Disk**<p> ```dh -h```</p>
